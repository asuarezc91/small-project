/*
All material copyright ESRI, All Rights Reserved, unless otherwise specified.
See https://js.arcgis.com/4.18/esri/copyright.txt for details.
*/
import{glsl as e}from"../views/3d/webgl-engine/core/shaderModules/interfaces.js";import{Transform as r}from"../views/3d/webgl-engine/core/shaderLibrary/Transform.glsl.js";import{ShaderBuilder as o}from"../views/3d/webgl-engine/core/shaderModules/ShaderBuilder.js";import{ColorConversion as a}from"../views/3d/webgl-engine/core/shaderLibrary/util/ColorConversion.glsl.js";import{Slice as i}from"../views/3d/webgl-engine/core/shaderLibrary/Slice.glsl.js";import{defaultMaskAlphaCutoff as d}from"../views/3d/webgl-engine/core/shaderLibrary/util/AlphaDiscard.glsl.js";function t(t){const l=new o;return l.include(r,{linearDepth:!1}),l.vertex.uniforms.add("proj","mat4").add("view","mat4"),l.attributes.add("position","vec3"),l.attributes.add("uv0","vec2"),l.varyings.add("vpos","vec3"),l.vertex.uniforms.add("textureCoordinateScaleFactor","vec2"),l.vertex.code.add(e`
    void main(void) {
      vpos = position;
      vTexCoord = uv0 * textureCoordinateScaleFactor;
      gl_Position = transformPosition(proj, view, vpos);
    }
  `),l.include(i,t),l.fragment.uniforms.add("tex","sampler2D"),l.fragment.uniforms.add("opacity","float"),l.varyings.add("vTexCoord","vec2"),7===t.output?l.fragment.code.add(e`
    void main() {
      discardBySlice(vpos);

      float alpha = texture2D(tex, vTexCoord).a * opacity;
      if (alpha  < ${e.float(d)}) {
        discard;
      }

      gl_FragColor = vec4(alpha);
    }
    `):(l.fragment.include(a),l.fragment.code.add(e`
    void main() {
      discardBySlice(vpos);
      gl_FragColor = texture2D(tex, vTexCoord) * opacity;

      if (gl_FragColor.a < ${e.float(d)}) {
        discard;
      }

      gl_FragColor = highlightSlice(gl_FragColor, vpos);
      ${t.OITEnabled?"gl_FragColor = premultiplyAlpha(gl_FragColor);":""}
    }
    `)),l}var l=Object.freeze({__proto__:null,build:t});export{l as I,t as b};
