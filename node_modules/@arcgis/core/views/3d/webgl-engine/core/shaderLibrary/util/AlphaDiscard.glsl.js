/*
All material copyright ESRI, All Rights Reserved, unless otherwise specified.
See https://js.arcgis.com/4.18/esri/copyright.txt for details.
*/
import{glsl as a}from"../../shaderModules/interfaces.js";const d=.1,o=.001;function r(d,o){const r=d.fragment;switch(o.alphaDiscardMode){case 0:r.code.add(a`
        #define discardOrAdjustAlpha(color) { if (color.a < ${a.float(.001)}) { discard; } }
      `);break;case 1:r.code.add(a`
        void discardOrAdjustAlpha(inout vec4 color) {
          color.a = 1.0;
        }
      `);break;case 2:r.uniforms.add("textureAlphaCutoff","float"),r.code.add(a`
        #define discardOrAdjustAlpha(color) { if (color.a < textureAlphaCutoff) { discard; } else { color.a = 1.0; } }
      `);break;case 3:d.fragment.uniforms.add("textureAlphaCutoff","float"),d.fragment.code.add(a`
        #define discardOrAdjustAlpha(color) { if (color.a < textureAlphaCutoff) { discard; } }
      `)}}export{r as DiscardOrAdjustAlpha,d as defaultMaskAlphaCutoff,o as symbolAlphaCutoff};
